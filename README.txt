Easy steps to run the project:
1. Make sure you have PostgreSQL DB installed
2. go to folder "backend" and run "DBconfig.exe", follow the instructions 
3. go to folder "backend" and run "VikingsData.exe", follow the instructions
4. go to folder "fronted" and run the website